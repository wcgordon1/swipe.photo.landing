---
const messages = [
"cleared so much space",
"phone feels lighter",
"finally organized photos",
"deleted the past",
"kept what matters",
"less clutter, more calm",
"stopped hoarding screenshots",
"freed up storage",
"goodbye blurry pics",
"only the good ones",
"let go already",
"closure feels good",
"moved on",
"over my ex",
"no more doom scrolling",
"peaceful camera roll",
"clean slate",
"memories, not mess",
"felt weirdly therapeutic",
"that felt great",
"why did I keep that?",
"out with the junk",
"instant relief",
"mental declutter",
"decision fatigue gone",
];
const marqueeItems = [...messages, ...messages]; // Duplicate for seamless loop
---

<section class="border-white/20 border-y">
  <div class="overflow-hidden py-2 relative text-white">
    <!-- Edge gradient overlays -->
    <div aria-hidden="true" class="absolute inset-0 z-10 pointer-events-none">
      <div
        class="absolute left-0 w-24 h-full bg-gradient-to-r from-base-950 via-base-950/90"
      >
      </div>
      <div
        class="absolute right-0 w-24 h-full bg-gradient-to-l from-base-950 via-base-950/90"
      >
      </div>
    </div>

    <!-- Marquee content -->
    <div
      class="flex w-max animate-marquee gap-6 whitespace-nowrap font-black tracking-widest uppercase"
    >
      {
        marqueeItems.map((msg, index) => (
          <>
            <span>{msg}</span>
            {index !== marqueeItems.length - 1 && (
              <span aria-hidden="true">✺</span>
            )}
          </>
        ))
      }
    </div>
  </div>
</section>
